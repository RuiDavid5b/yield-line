info:
  name: filing_signals
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/companies/{{cik}}/filing-signals"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: contains
      value: "\tapplication/json"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
